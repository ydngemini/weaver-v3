# Weaver on AWS — provider config.
# Single EC2 Graviton box running the same systemd stack as the Oracle kit.
terraform {
  required_version = ">= 1.5"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = var.region
}
